# Epigram Spring Site
Serves epigrams in a site like `fortune` in Unix.
Comes with existing epigrams! But you are also 
free to add your own.

## Setup
1. Create a .env file following the format of `.env.example`.
2. Run `docker compose up --build`
3. You should now be able to access the site at `localhost:8080`
